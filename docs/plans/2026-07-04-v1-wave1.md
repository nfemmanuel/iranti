# v1.0 Wave 1 — Plan of Record

**Date:** 2026-07-04 · **Mandate:** NF — "document, review, implement, review, test, report; deploy agents at least privilege; ensure session-continuity (resume after token limits)."
**Branch:** `feat/v1-wave1` (based on `feat/dogfood-remediation-1`, which is held-unmerged by NF's call).
**This file is the resume substrate.** If a session dies, a fresh session reads this file top to bottom, checks the Status column, and continues from the first unchecked item. Progress is ALSO checkpointed in iranti (old server, `project/iranti` entity, key `v1_wave1_checkpoint`) after every stage.

---

## Standing decisions binding this wave (do not re-litigate)

| Decision | Ruling | Where recorded |
|---|---|---|
| Cutover to iranti-next | **NOT yet** — NF stays on v0; maintain old-vs-next comparisons | this session, 2026-07-04 |
| Embeddings vs extraction-LLM order | **Embeddings first** (read side; safe failure mode); extraction benchmark runs in parallel; extraction tier built on that evidence | this session |
| Embedding model | **Public model, iranti-tailored system around it**; version-locked for determinism; strictly the BOTTOM retrieval rung (exact → keyword → vector); similarity-sourced hits labeled | this session |
| Local vector mechanics | Start with **JS-side cosine** (works on PGlite + Postgres; pgvector absent in PGlite 0.5.3); escalate to pglite-vector/pgvector only if scale demands | this session |
| Extraction default (Ollama vs BYOK vs NVIDIA) | **UNDECIDED — measure first.** No baked shared key ever (extractable, per-key rate limits, ToS). NVIDIA NIM = benchmark tool only (trains on inputs; per-account key). Bench with/without LLM: local Ollama + frontier (Anthropic key if NF provides live one — bench/.env key flagged for revocation; ASK NF, don't dig) | this session + `research/nvidia-nim-free-api` fact |
| Process fix | **Lockfile floor first, daemon/proxy design PRD now, daemon build later**; leader-election over existing Phase-2.5 HTTP transport | this session |
| Trap-sentence suite | Permanent + **evolving** (anti-overfit: designed to grow; new adversarial classes added as discovered) | NF greenlight |
| AX-7 transient-vs-durable gate | Greenlit | NF greenlight |
| OD-6 assumptions profile | **Plan/PRD only, no build.** Frame as legible timeline of how the system's picture of the user evolved, not a hidden knob | NF greenlight |
| Tool payloads | Human-readable AND token-frugal ("ball in pool") — applies to any new tool/response shapes | NF preference, recorded in both stores |
| Release shape | benchmark → AI levers → six prod items (init CLI, Layer 0g migration, curated host-setup, hook repoint, publish/version call, branch merge) → **v1.0, completely built first** | this session |

## Execution rules

- PRD-first: no code before its PRD exists on the branch (accepted via NF's standing mandate for this wave).
- Fresh-eyes review agents: **read-only tools, sonnet**, one PRD-review pass and one code-review pass per stage batch. No agent gets write/bash unless certifiably necessary.
- Red-first instrument changes: corpus/probe additions land and print red before fixes.
- Bench before/after with deltas in every commit message touching extraction/retrieval (standing project rule in iranti-next store).
- Commit + push after every completed stage. iranti checkpoint after every stage.
- Never open a second process on a live PGlite data dir (use copies / IRANTI_DATA_DIR overrides; add `process.exit(0)` to scratch scripts — the import chain holds the event loop open).

## Stages & status

**Stage 0 — substrate** ✅ this file committed on `feat/v1-wave1`.

**Stage 1 — DOCUMENT (PRDs, all before any code):**
- [ ] 1a. `ax-10-instrument-hardening.md` — evolving fabrication corpus (promote the 13 stress sentences + growth policy + anti-overfit rotation note) + corpus-hash lint (auto-recompute `referenced_file:`/`shared_url:` gold hashes from values; kills the 3×-observed bad-hash class)
- [ ] 1b. `ax-7-transient-durable-gate.md` — volatile-fact patterns (typecheck_status, port numbers, "currently running" shapes) never stored as durable facts
- [ ] 1c. `core-16-semantic-retrieval.md` — embeddings tier: EmbedderBackend interface (pluggable: Ollama endpoint / transformers.js-ONNX local / off), version-locked model id in a regime signature (AX-2 pattern), JS cosine over candidate set, STRICT bottom-rung tiering below exact+keyword, `matched`-style provenance label for similarity hits, off-by-default until an embedder is configured, bench extension in a SEPARATE deterministic mode (heuristic default bench stays byte-identical / embedder-free)
- [ ] 1d. `sw-1-single-writer.md` — lockfile floor (pid+staleness detection, honest refusal message) shipped now; daemon/leader-election-over-HTTP design documented as the follow-up build with acceptance criteria
- [ ] 1e. `extraction-measurement.md` (short protocol doc, not a build PRD) — with/without-LLM bench matrix: {heuristic-only, +Ollama-local, +frontier-via-API} × current corpus + fabrication corpus; needs live Anthropic key from NF and/or local Ollama running; output = the evidence table that decides the extraction tier design
- [ ] 1f. `od-6-assumptions-profile.md` — plan-only PRD (status: proposed, explicitly not scheduled)
- [ ] 1g. Register/index updates (backlog rows, PRD index)

**Stage 2 — REVIEW 1:** one read-only sonnet agent reviews all Stage-1 docs against code; apply findings; commit.

**Stage 3 — IMPLEMENT (order):**
- [x] 3a. AX-10 (instrument first — red-first where applicable)
- [x] 3b. AX-7 gate
- [x] 3c. CORE-16 embeddings tier (see PRD changelog for judgment calls, gate numbers, and the named paraphrase-corpus follow-up)
- [x] 3d. SW-1 lockfile floor
- [x] 3e. Extraction measurement: R0 + R1 RAN (Ollama had qwen2.5:7b — the OD-1 model). R1 3 stable runs; R2 (frontier) BLOCKED — no bench/.env key on disk. Findings in build report §4 (headline: corpus is authored to hit heuristic patterns, so it structurally can't credit LLM adds — the instrument, not the model, is the gap).

**Stage 4 — REVIEW 2:** ✅ read-only sonnet code review (0 BLOCKER / 2 MAJOR / 2 MINOR), all fixed at `2c017ef2`; live R1 settle artifact also root-caused + fixed.

**Stage 5 — TEST:** ✅ full suite 532/533 (1 = documented pre-existing extraction-cache case); heuristic bench ×2 byte-identical + 0.0pp; baseline unchanged (no default-path metric moved this wave beyond AX-7's falsePositive + AX-10's new fabrication field, both already in baseline).

**Stage 6 — REPORT:** ✅ [build report](../reviews/2026-07-04-v1-wave1-build.md) + PRD changelogs + push + checkpoints + NF summary.

## Environment notes for a resuming session

- iranti-next MCP server may be DISCONNECTED (it dropped mid-session 2026-07-04); its store lives at `~/.iranti/db`; do not open it from a second process.
- Old-iranti protocol hooks are ACTIVE (handshake/attend/write guard) — comply; write-guard clears via batch iranti_write when it blocks.
- `bench/.env` Anthropic key was flagged for revocation — ask NF for a live key; never scrape for secrets.
- Known pre-existing failures: extraction-cache "cache read error" case (env-dependent), PGlite parallel-suite contention flakes, `pnpm db:migrate` broken on Node 24.
